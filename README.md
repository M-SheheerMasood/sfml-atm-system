SFML C++ ATM Application:
  Includes a GUI and backend logic for inputs using the SFML library in C++.
Core:
  Using SFML built-in objects and OOPs to design and manage the flow.
  Built a GUI from scratch, highly responsive and interactive.
  Built as a team of 2, where I was the primary developer for the SFML interface, OOP architecture, and logical flow.
To run:
  Download the release and run "main.exe" - only needs C++ installed in system
